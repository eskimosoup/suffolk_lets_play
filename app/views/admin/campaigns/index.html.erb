<% title Campaign.model_name.human.pluralize %>

<div class="admin_functions">
  <%= link_to "New #{ Campaign.model_name.human }", new_admin_campaign_path %>
</div>

<%= informant_table @campaigns, {
      :headings => [ 'Name', 'Slug', 'Logo', '', '' ], 
      :body => [ :name, :slug, lambda{|x| image_tag(x.logo.thumb) if x.logo?}, it_edit, it_destroy ]
    } %>
